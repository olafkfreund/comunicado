//! WebSocket server stub for mobile communication
//! TODO: Implement WebSocket server functionality

pub struct WebSocketServer;

impl WebSocketServer {
    pub fn new() -> Self {
        Self
    }
}

impl Default for WebSocketServer {
    fn default() -> Self {
        Self::new()
    }
}